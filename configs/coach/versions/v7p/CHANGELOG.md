# v7p — v7 + evidence enforcement: playbook, settled ledger, ledger veto, curriculum lock (hand-written, 2026-09-08)

parent: v7
hypothesis: the same four devices as v5p (cross-run playbook, settled curriculum ledger, ledger
veto, curriculum lock = v7's rule enforced) on top of v7's "never lower target_ms on a rising trend" rule — queued so
that the candidate is valid whichever of v5/v7 is the incumbent after generation 2:
rough-medium-naive +0.5 (breakout recipe), rough/stairs +0.05 (curriculum release no longer
refused on the biased immediate row, penalty escalation against the ledger vetoed).
expected_delta_j: 0.15

## Rationale
See configs/coach/versions/v5p/CHANGELOG.md — identical devices, parent v7 instead of v5.
The playbook/settled/veto paragraphs are inserted after the v5 evidence paragraph of
system.md exactly as in v5p; v7's CRITICAL RULE paragraph is kept verbatim. user.md = v7.

## Code ideas (not applied)
- (none)
